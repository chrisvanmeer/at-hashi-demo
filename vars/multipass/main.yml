---
multipass_instances:
  servers:
    - server1
    - server2
    - server3
  clients:
    - client1
    - client2
    - client3
    - client4

image_name: "jammy"

cloud_init_location: "~"
cloud_init_full_path: "{{ cloud_init_location }}/at-cloud-init.yml"
public_key: "~/.ssh/id_rsa.pub"

multipass_passphrase: "atcomputing"